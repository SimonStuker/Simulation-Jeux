[package]
name = "common"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
fastrand = "2"
rayon = { version = "1.11.0", optional = true }

[features]
default = ["parallel"]
parallel = ["dep:rayon"]
